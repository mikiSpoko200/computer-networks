#![allow(dead_code)]

mod registry;
mod segment;
mod util;
mod messages;
mod window;
mod downloader;

use libc;

use downloader::Downloader;

fn main() {
    // let address =
    // let downloader = Downloader::new();
}
